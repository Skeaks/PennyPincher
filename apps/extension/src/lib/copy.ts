/**
 * The consent copy moved to `src/copy/strings.ts` in S15, where every user-facing string now
 * lives. This re-export keeps the S04 import path working for the consent entrypoint.
 */
export { CONSENT_COPY, NETWORK_REQUESTS } from "../copy/strings";
