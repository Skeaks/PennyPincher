# S15 outcome, 2026-09-07

Shipped (as a draft, over two tests; see below). The carried-in items first: every user-facing
string in the extension now lives in `src/copy/strings.ts` (`lib/copy.ts` is a re-export so
the consent entrypoint, outside `owns:`, is untouched); `NETWORK_REQUESTS` lists the five
requests (retailer page, 15-minute upload, ladder query, daily health counts, delete) with a
consent sentence and an options-page clause each; `howItWorks` is derived from it as a
numbered list ("Request n of 5, to ..."); the options page's "Sent anywhere" paragraph is the
same list joined; `CONSENT_VERSION` is 6; `PANELIST_IDS_KEPT` is 13. `test/consent.test.ts`
now reads `sync/transport.ts` with comments stripped and fails if the set of `/v1/...` paths in
the code differs from the set the copy declares, or if a `fetch`/`postJson` call site carries
a path that is not listed; a second scan walks every `src/**/*.{ts,html}` file for the rule-10
words (whole lines in `src/copy/`, string literals elsewhere) and fails unless the line before
is exactly `// claims-reviewed`. Then the pack: `docs/compliance/privacy-policy.md` (every
schema 1.1.0 field in a table with its source, the four server requests, the rotating id, the
retention table, the deletion path, the waitlist, five questions for counsel) and `consent.md`
(the screen line by line, version history), both marked DRAFT; `docs/webstore.md` (listing
text, the privacy-practices answers, one paragraph per permission, the unlisted procedure,
Jamie's pre-submission checklist; `pnpm --filter extension zip` already worked and emits
`.output/extension-0.1.0-chrome.zip`); `apps/web` (static `public/`, one Pages Function
`POST /api/waitlist` over its own D1 `waitlist(email, joined_at)` table, the pure module tested
with 20 cases, and a copy test that scans the shipped HTML for regulated words and the three
ideas the brief names). The gate runs 931 tests after this (the per-file regulated-words scan
is one test per source file). The Pages job for `deploy.yml` is its own PR (rule 4), with the
`docs/deploy.md` paragraph.

Why it is a draft. `PANELIST_IDS_KEPT` went from 4 to 13 as the brief says, and two S14 tests
outside `owns:` pin 4: `test/identity/panelist.test.ts` ("keeps the last four ids", six mints
expecting four) and `test/identity/delete.test.ts` ("older than the identity history", six
weeks expecting a history of four). Same situation as the S06 and S14 posture pins, same
handling: the tests fail honestly, the PR body proposes the two edits (mint 15, expect the
last 13; and 15 weeks for the delete test so two ids still survive only on stored rows), and
Jamie applies them. Jamie approved the same day (2026-09-08) and the edits were applied on that
instruction; no agent edited a test it does not own on its own. Gate green after that, 931
tests. Noticed while rerunning: `test/sync/sync.test.ts` "stops at the first failed batch" ran
at 3.6 s under the full gate (5 s limit) and timed out twice on this machine under load; it is
a pre-existing margin, not touched here, and worth a look in S16.

What the brief got wrong. (1) "Form posts to a Worker route" reads as `apps/api`, which is not
in `owns:` and would put an email next to the observations; a Pages Function in `apps/web`
with its own D1 database is a Worker route too, keeps the email in a separate database (the
brief's "separate table" made stricter), and stays inside `owns:`. The database id in
`apps/web/wrangler.toml` is a placeholder until Jamie creates the database; the deploy job
fails loudly until then. (2) `docs/webstore.md` is not under `docs/compliance/**` but the brief
names it, so it was written. (3) The retention doc says "if the two ever disagree, fix both in
one PR", so its "four ids" sentences were corrected here, outside `owns:`, called out in the
PR. (4) "The PR label" half of the regulated-words rule cannot be tested offline; the test
checks the marker line, and the marker is documented as Jamie's to write alongside the label.

What was harder than expected. A NUL byte appeared inside a string literal in the new test
(the second time this machine has done that, S12 was the first); Biome's diff rendered it as
`␀`, and the whole tree of changed files is now scanned with `perl -ne` before committing.
The regulated-words scan had to distinguish `save:` the identifier (`capture/health.ts`) from
"save" the claim, hence string literals only outside `src/copy/`. The consent page renders
`howItWorks` as a flat list, so the enumeration is numbered in the text rather than nested.

Follow-ups. `deleteOutcomeText` in `identity/delete.ts` still carries its own strings (outside
`owns:`; the whole-tree scan covers it). No Turnstile on the waitlist form, only a honeypot;
fine for an unlisted pilot link. The `product.url` query-string question is for counsel
(privacy policy, section 12). The 128x128 store icon does not exist. `docs/sessions/README.md`
needs the S15 row.
