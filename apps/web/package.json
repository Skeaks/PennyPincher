{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "description": "PennyPincher landing page and pilot waitlist. Static HTML on Cloudflare Pages plus one Pages Function that stores a waitlist email in its own D1 table.",
  "type": "module",
  "scripts": {
    "dev": "wrangler d1 migrations apply WAITLIST_DB --local && wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name pennypincher-web",
    "db:migrate:local": "wrangler d1 migrations apply WAITLIST_DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply WAITLIST_DB --remote",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260904.1",
    "typescript": "5.7.3",
    "vitest": "3.0.5",
    "wrangler": "4.129.0"
  }
}
