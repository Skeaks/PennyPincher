{
  "name": "@pennypincher/synth",
  "version": "0.1.0",
  "private": true,
  "description": "Synthetic price ladders with known ground truth. Generates valid PriceObservations from a chosen tier distribution so the stats engine can be tested against answers known by construction.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pennypincher/schema": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.10.5"
  }
}
