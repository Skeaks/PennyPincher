{
  "name": "@pennypincher/schema",
  "version": "0.1.0",
  "private": true,
  "description": "The observation contract. Every price the panel captures is validated against this before it is stored. Versioned; bump SCHEMA_VERSION and add a docs/migrations entry on any breaking change.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "3.24.1"
  },
  "devDependencies": {
    "@types/node": "22.10.5"
  }
}
