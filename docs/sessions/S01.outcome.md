# S01 outcome, 2026-09-04

Shipped: four bad PRs (#1 to #4), each rejected by exactly the intended CI step, recorded in
`docs/audit/S01-gate-self-test.md`; all four closed and their branches deleted.
`.github/workflows/nightly.yml` (04:00 UTC, `pnpm gate`, commented mutation and E2E placeholders,
opens or appends to a `nightly failed` issue via `actions/github-script`).

What the brief got wrong: nothing material. The retro file lives in `docs/sessions/`, which is
outside this session's `owns:` list, yet CLAUDE.md requires it. Briefs should list
`docs/sessions/<id>.outcome.md` in `owns:` by default.

What was harder than expected: the fake AWS key had to be chosen carefully. Gitleaks only
matches `AKIA` followed by base32 (`[A-Z2-7]{16}`) and allowlists keys ending in `EXAMPLE`, so
the obvious `AKIAIOSFODNN7EXAMPLE` would have passed and produced a false green. Also, the
session branch was created by the desktop app as `claude/gate-self-test-6f573f` rather than the
`s01/gate-self-test` naming in CONTRIBUTING.md; left as is rather than renaming a branch the app
tracks.

Follow-up for Jamie: `nightly.yml` uses the `nightly` label. GitHub creates it on first use, but
creating it up front alongside `claims-reviewed` and `fixtures-reviewed` avoids surprise.
Consider a manual `workflow_dispatch` run after merge to confirm the job itself is healthy.
