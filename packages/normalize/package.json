{
  "name": "@pennypincher/normalize",
  "version": "0.1.0",
  "private": true,
  "description": "Product identity: the same banana under three retailers' ids. UPC to GTIN-14 when a page shows one, otherwise token-set similarity over brand + title + size with units normalised. Measured against a hand-labelled golden set of same/different pairs.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pennypincher/schema": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "zod": "3.24.1"
  }
}
