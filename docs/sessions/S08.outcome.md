# S08 outcome, 2026-09-04

Shipped: `.github/workflows/deploy.yml` (push to `main` touching `apps/api/**`, `packages/**`
or the workflow deploys production; `deploy-staging` label deploys a PR head to staging on
label and on every push; `workflow_dispatch` for either; migrations then atomic deploy then
poll `/healthz` for the SHA and print the measured time), `docs/deploy.md`, the drill
procedure in `docs/audit/S08-deploy-drill.md`. Separately, #19: `build` in `/healthz` and the
`PILOT_TOKEN` bearer check on ingest (constant-time; open when the secret is unset).

What the brief got wrong: (1) Two of its acceptance items (`build` in `/healthz`, the bearer
token) need `apps/api` changes, but `owns:` lists none and rule 4 forbids `.github/` and
`apps/` in one PR, so the session is two PRs and the deploy PR cannot be verified until the API
PR merges. Future gate-plus-code briefs should say so. (2) "Cloudflare prerequisites" needs a
fifth item: register the account's workers.dev subdomain (Workers & Pages > onboarding, or the
first `wrangler deploy` run interactively). Without it `wrangler deploy` uploads the Worker
and then fails, non-interactively, with "You need to register a workers.dev subdomain".

What was harder than expected: gitleaks flagged a 32-hex test token in a test file, and since
it scans every PR commit the fix was a fresh branch (#18 closed, #19 opened), not a force push.
`git add -A` on a branch with an untracked workflow file next to it tripped boundary-guard
once; keep the two branches in separate worktrees next time. The permission classifier
blocked `gh pr merge` in this session, so #19 waits for Jamie despite green CI.

Measured: staging, label to live, 54 s (run 33920012104); the migration step reported "No
migrations to apply!" on the second run, so it is idempotent in practice. Drill done
(`docs/audit/S08-deploy-drill.md`): a zeroed D1 id fails the migration step, nothing uploads,
staging keeps serving. The production number is left TBD: the merge of #21 is the first
production deploy, and its run summary prints the merge-to-live seconds. Whoever merges it
should copy that number into `docs/deploy.md`. Stale branches to delete on origin:
`s08/healthz-build`, `s08/drill-bad-config`.
