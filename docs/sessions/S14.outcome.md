# S14 outcome, 2026-09-07

Shipped as a DRAFT (one failing test, Jamie's to change; see below). `apps/api`: migration
0002 (index on `(panelist_id, observed_at)`, `rate_buckets`, `panelist_flags`); the POST now
runs validate, rate-limit check, semantic dedup, store, count, abuse check. Rate limit is
1,000 rows an hour per bearer (keyed on a SHA-256 prefix, never the token) and per
`panelistId`, fixed windows, `429` + `Retry-After`, a refused batch stores and counts nothing.
Semantic dedup folds the same panelist + cell + price + currency within ten minutes of
`observedAt` into `duplicates`, not stored, stable on resend. The abuse guard flags a panelist
whose every price in a cell is outside three-plus other panelists' range by more than 50%,
logs it, keeps the rows, and `GET /v1/cells` excludes the panelist until a human sets the flag
to `cleared`. `DELETE /v1/panelists/:id` removes observations, flag and buckets, idempotent. A
daily cron purges raw rows and flags at 90 days, rate windows at 2 h. `docs/data-retention.md`
written. `apps/extension/src/identity/`: rotation every 7 days keeping four ids (`pp:panelist`
shape unchanged, retired ids in `pp:panelist-history`); "Delete my data" collects every id
(identity plus any still on stored rows), calls the endpoint for each, and clears local state
only when every call succeeded. End-to-end test drives the real Hono app on the in-memory
repo from the extension's sync and delete. 26 new API tests, 14 new extension tests.

Why it is a draft: `test/probe/posture.test.ts` pins `sync/transport.ts` to exactly two fetch
call sites; the delete is a third. Same situation as S11, same handling: the test fails
honestly, the PR body proposes `toHaveLength(3)`. Biome's line wrapping made the pin pass by
accident at first (the regex wants `fetch(\`` on one line); the call site was rewritten to
the pinned form so it fails as it should. No agent edited that test.

What the brief got wrong: `owns:` names `apps/extension/src/identity/**` but the panelist id
lived in `capture/panelist.ts` (now a re-export), the delete button in `entrypoints/options/`,
the request in `sync/transport.ts`, and the consent copy had to change (it promised the delete
"in a later version"), which bumps `CONSENT_VERSION` to 5: a third request to our own server
is network behaviour the copy describes. "Keeps the last 4" and "every id it has ever used"
conflict once raw retention is 90 days (13 rotations); the extension keeps four and also names
any id still on a stored row, and the retention doc records the gap. Raising
`PANELIST_IDS_KEPT` to 13 closes it: Jamie's call. Two S07 ingest tests posted 200 identical
views of one price and expected 200 accepted; that is the repeat view S14 must fold, so their
fixtures now vary the SKU (assertions unchanged), called out in the PR. With one pilot token
the per-token limit is a panel-wide 1,000 rows an hour; fine for 30 to 60 panelists at
15-minute syncs, not for more. The brief did not say what "outside the observed range" means
with one or two other panelists; the guard needs three so a two-price ladder is never a fraud.

What was harder than expected: the extension test wanted the real API, which is not a
dependency of the extension; `api` is now a workspace devDependency of `extension` (test
only), and the extension's stricter tsconfig typechecks the API sources it imports, which
happened to be clean. Bash heredocs failed to write TypeScript on this machine again (S07
noted it); files went through the editor tool.

Not done, deliberately: no admin endpoint for reviewing flags (a `wrangler d1 execute` in the
retention doc); the cells cache is not purged on delete (60 s); the options page's "Sent
anywhere" paragraph still describes only the probe request (S11 copy, Jamie's). Follow-ups
for Jamie: the posture pin; `PANELIST_IDS_KEPT`; mark S14 done in `docs/sessions/README.md`.
