{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "description": "PennyPincher ingest API. Hono on Cloudflare Workers with D1: accepts an ObservationBatch, validates it against @pennypincher/schema (PII guard included), stores it idempotently.",
  "type": "module",
  "scripts": {
    "dev": "wrangler d1 migrations apply DB --local && wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pennypincher/schema": "workspace:*",
    "hono": "4.13.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260904.1",
    "typescript": "5.7.3",
    "vitest": "3.0.5",
    "wrangler": "4.129.0"
  }
}
